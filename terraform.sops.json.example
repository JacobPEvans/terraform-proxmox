{
  "_comment": "SOPS-encrypted network topology. Copy to terraform.sops.json, fill in real values, then: sops --encrypt --in-place terraform.sops.json. See docs/SOPS_SETUP.md. All other config lives in deployment.json (plaintext, committed).",

  "network_prefix": "192.168.0",
  "vm_ssh_public_key_path": "~/.ssh/id_rsa_vm.pub",
  "vm_ssh_private_key_path": "~/.ssh/id_rsa_vm"
}
