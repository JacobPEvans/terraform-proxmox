name: Code Simplifier

on:
  schedule:
    - cron: "0 4 * * *"
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

jobs:
  simplify:
    uses: JacobPEvans/ai-workflows/.github/workflows/code-simplifier.yml@v0.2.9
    secrets: inherit
