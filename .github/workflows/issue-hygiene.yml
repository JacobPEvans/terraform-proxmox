name: Issue Hygiene

on:
  schedule:
    - cron: "0 7 * * 1"
  workflow_dispatch:

permissions:
  contents: read
  issues: write
  pull-requests: read

jobs:
  hygiene:
    uses: JacobPEvans/ai-workflows/.github/workflows/issue-hygiene.yml@v0.2.3
    secrets: inherit
