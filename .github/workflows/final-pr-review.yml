name: Final PR Review

on:
  pull_request_review:
    types: [submitted]
  check_suite:
    types: [completed]

permissions: {}

jobs:
  review:
    uses: JacobPEvans/ai-workflows/.github/workflows/final-pr-review.yml@v0.1.0
    secrets: inherit
