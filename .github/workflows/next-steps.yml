name: Next Steps

on:
  schedule:
    - cron: "0 5 * * *"
  workflow_dispatch:

permissions:
  contents: write
  issues: write

jobs:
  analyze:
    uses: JacobPEvans/ai-workflows/.github/workflows/next-steps.yml@v0.1.0
    secrets: inherit
