name: Next Steps

on:
  schedule:
    - cron: "0 5 * * *"
  workflow_dispatch:

permissions:
  contents: read
  issues: write

jobs:
  analyze:
    uses: JacobPEvans/ai-workflows/.github/workflows/next-steps.yml@v0.2.6
    secrets: inherit
